# The vendored Claude Code CLI ships a large native binary (~239M, linux-x64).
# Store it via Git LFS so it doesn't bloat the repo history.
vendor/claude-cli/node_modules/@anthropic-ai/claude-code-linux-x64/claude filter=lfs diff=lfs merge=lfs -text
